"use client";
import React, { useState } from "react";

type PackingListItemProps = {
  data: {
    id: number;
    title: string;
    isPacked: boolean;
  };
};

export default function PackingListItem() {
  return <div>HeY YYYY </div>;
}
